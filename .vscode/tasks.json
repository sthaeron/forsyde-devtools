{
    "tasks": [
        {
            "type": "shell",
            "label": "Run ForSyDe LSP",
            "command": "./run.sh ${file}",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Run Haskell LSP and klighd client"
        }
    ],
    "version": "2.0.0"
}
